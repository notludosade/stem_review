# Notes

- First course in the new "Advanced+ Courses" category — scoped strictly to fundamentals per the user's explicit instruction, not a full graduate-level treatment. See "Out of scope" in MISSION.md.
- Static SVG figures only (no draggable widgets) — proof-based content is about logical structure, not explorable function behavior, so this course doesn't need `assets/interactive.js`.
